# GitHub Publish Checklist

Before creating the public GitHub repository:

- [ ] Rename notebook title and remove assignment-only wording if desired.
- [x] Create a portfolio notebook that keeps key outputs and removes long package installation logs.
- [ ] Replace hard-coded or masked API key cells with environment variable examples.
- [ ] Decide whether original PDF manuals can be redistributed.
- [ ] Exclude Chroma vector database files from GitHub.
- [ ] Add source links for camera manuals in `data/README.md`.
- [ ] Add a short demo section with 2-3 example questions and answers.
- [ ] Add screenshots only if they do not expose private paths or keys.
- [ ] Run notebook once in a clean Colab environment after changes.
